import { RaizQuadradaPipe } from './raiz-quadrada.pipe';

describe('RaizQuadradaPipe', () => {
  it('create an instance', () => {
    const pipe = new RaizQuadradaPipe();
    expect(pipe).toBeTruthy();
  });
});
